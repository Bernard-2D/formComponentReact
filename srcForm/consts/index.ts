export enum Status {
  enable,
  disable
}

export enum MenuType {
  module = 1,
  menu,
  button
}

export enum OrderType {
  desc = 'DESC',
  asc = 'ASC'
}

export const SystemKey = 'system'

export const ADMIN = 'superAdmin'
